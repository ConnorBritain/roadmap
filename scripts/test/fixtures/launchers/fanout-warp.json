{
 "status": 0,
 "stdout": "# roadmap fanout \u2014 wave 1, terminal=warp (Tab Config + warp://tab_config deeplink)\n$ErrorActionPreference = 'Continue'\ngit fetch origin --quiet\n\n# 1) one worktree + uncommitted kickoff brief per slice\ngit worktree add \"<ROOT>/_wt/auth-s1\" -b \"auth/s1\" origin/main 2>$null; if ($LASTEXITCODE -ne 0) { Write-Host \"worktree <ROOT>/_wt/auth-s1 exists, reusing\" }\nSet-Content -LiteralPath \"<ROOT>/_wt/auth-s1/.kickoff.md\" -Encoding utf8 -Value @'\n# Kickoff \u2014 auth-login  (AUTH \u00b7 S1)\n\n> Uncommitted brief for this fanout session. You own the atomic sequence\n> **read \u2192 plan \u2192 build \u2192 test \u2192 commit \u2192 push \u2192 open PR**. Do **NOT** merge \u2014 the lead merges.\n\n**Slice:** `auth-login`  \u00b7  **Branch:** `auth/s1`\n**What:** build login\n\n**Resuming?** `roadmap linear notes auth-login` shows any prior-session notes on this slice's tracker issue \u2014 read them first if present (that's where a dead session left off).\n\n## 0.5 Author instructions (verbatim)\nStart from the failing test.\n\n## 1. Scope / target\n- `src/login.ts`\n\n## 2. Read-order (orient first \u2014 paths are relative to `docs/`; you're at the repo root, so e.g. `sprints/...` = `docs/sprints/...`, and `../STATUS.md` = `STATUS.md`)\n_(see the slice's detail entry in docs/SLICES.md)_\n\n## 3. Next action\n(see the slice detail entry)\n\n## 4. Verification gate (must pass before commit)\n```\nnpm test\n```\n\n## 5. Commit + PR\n- Commit style: `<area>: <what> (auth-S1)`, professional, no AI attribution.\n- Open a PR `--base main --head auth/s1` whose description includes the exact line `roadmap: slice=auth-login` (the reconcile marker). **Do NOT merge.**\n\n## 6. Report back\nLOC delta \u00b7 file inventory \u00b7 gate result (pass/fail with output) \u00b7 commit SHA \u00b7 PR # \u00b7 2\u20133 line retro.\nJournal as you go: at each checkpoint \u2014 a gate cleared, a blocker hit, a logical unit done \u2014 post `roadmap linear note auth-login \"\u2026\" [--kind progress|blocker|done]` (a no-op if this slice isn't tracker-mapped), so if this session dies the next one resumes from the trail, not from zero.\nLeftovers: before opening the PR, file anything that genuinely blocks or follows this slice (discovered bugs, broken contracts, deferred gate items) to the BACKLOG ONLY \u2014 the `backlog_add` MCP tool if available, else `roadmap backlog add \"<title>\" -k followup --slice auth-login`, else a **Leftovers** heading in the PR body (the lead's /sync harvests it). NEVER add sprints or PIs from this session \u2014 scope decisions belong to the human. Skip speculative ideas entirely (YAGNI applies to captures too).\n'@\ngit worktree add \"<ROOT>/_wt/auth-s2\" -b \"auth/s2\" origin/main 2>$null; if ($LASTEXITCODE -ne 0) { Write-Host \"worktree <ROOT>/_wt/auth-s2 exists, reusing\" }\nSet-Content -LiteralPath \"<ROOT>/_wt/auth-s2/.kickoff.md\" -Encoding utf8 -Value @'\n# Kickoff \u2014 auth-sessions  (AUTH \u00b7 S2)\n\n> Uncommitted brief for this fanout session. You own the atomic sequence\n> **read \u2192 plan \u2192 build \u2192 test \u2192 commit \u2192 push \u2192 open PR**. Do **NOT** merge \u2014 the lead merges.\n\n**Slice:** `auth-sessions`  \u00b7  **Branch:** `auth/s2`\n**What:** tokens\n\n**Resuming?** `roadmap linear notes auth-sessions` shows any prior-session notes on this slice's tracker issue \u2014 read them first if present (that's where a dead session left off).\n\n## 1. Scope / target\n- `src/session.ts`\n\n## 2. Read-order (orient first \u2014 paths are relative to `docs/`; you're at the repo root, so e.g. `sprints/...` = `docs/sprints/...`, and `../STATUS.md` = `STATUS.md`)\n_(see the slice's detail entry in docs/SLICES.md)_\n\n## 3. Next action\n(see the slice detail entry)\n\n## 4. Verification gate (must pass before commit)\n```\nnpm test\n```\n\n## 5. Commit + PR\n- Commit style: `<area>: <what> (auth-S2)`, professional, no AI attribution.\n- Open a PR `--base main --head auth/s2` whose description includes the exact line `roadmap: slice=auth-sessions` (the reconcile marker). **Do NOT merge.**\n\n## 6. Report back\nLOC delta \u00b7 file inventory \u00b7 gate result (pass/fail with output) \u00b7 commit SHA \u00b7 PR # \u00b7 2\u20133 line retro.\nJournal as you go: at each checkpoint \u2014 a gate cleared, a blocker hit, a logical unit done \u2014 post `roadmap linear note auth-sessions \"\u2026\" [--kind progress|blocker|done]` (a no-op if this slice isn't tracker-mapped), so if this session dies the next one resumes from the trail, not from zero.\nLeftovers: before opening the PR, file anything that genuinely blocks or follows this slice (discovered bugs, broken contracts, deferred gate items) to the BACKLOG ONLY \u2014 the `backlog_add` MCP tool if available, else `roadmap backlog add \"<title>\" -k followup --slice auth-sessions`, else a **Leftovers** heading in the PR body (the lead's /sync harvests it). NEVER add sprints or PIs from this session \u2014 scope decisions belong to the human. Skip speculative ideas entirely (YAGNI applies to captures too).\n'@\n\n# 2) write the Warp Tab Config, then open it via the warp:// deeplink\n$cfgDir = Join-Path $env:APPDATA 'warp\\Warp\\data\\tab_configs'\nNew-Item -ItemType Directory -Force -Path $cfgDir | Out-Null\nSet-Content -LiteralPath (Join-Path $cfgDir 'roadmap-wave1.toml') -Encoding utf8 -Value @'\nname = \"roadmap-wave1\"\ncolor = \"blue\"\n\n[[panes]]\nid = \"root\"\nsplit = \"horizontal\"\nchildren = [\"lead\", \"slices\"]\n\n[[panes]]\nid = \"lead\"\ntype = \"terminal\"\nshell = \"powershell\"\ndirectory = '<ROOT>'\ncommands = [\"echo 'LEAD - review + merge each slice PR; workers do NOT merge'\"]\nis_focused = true\n\n[[panes]]\nid = \"slices\"\nsplit = \"vertical\"\nchildren = [\"s0\", \"s1\"]\n\n[[panes]]\nid = \"s0\"\ntype = \"terminal\"\nshell = \"powershell\"\ndirectory = '<ROOT>/_wt/auth-s1'\ncommands = [\"Write-Host 'auth-login: worktree and .kickoff.md ready; start your configured assistant here.'\"]\n\n[[panes]]\nid = \"s1\"\ntype = \"terminal\"\nshell = \"powershell\"\ndirectory = '<ROOT>/_wt/auth-s2'\ncommands = [\"Write-Host 'auth-sessions: worktree and .kickoff.md ready; start your configured assistant here.'\"]\n'@\nStart-Process \"warp://tab_config/roadmap-wave1?new_window=true\"\nWrite-Host \"Opened Warp tab config 'roadmap-wave1' - lead + 2 slice pane(s).\"\n",
 "stderr": "fanout: wave 1/1 \u00b7 cap 2 (recommended 1, bound by CPU) \u00b7 term=warp \u00b7 lane=max \u00b7 dry\nslices: auth-login, auth-sessions\n\n(--dry \u2014 nothing spawned. Drop --dry to launch; --out <file> to save the script.)\n"
}