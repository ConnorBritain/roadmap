# @connorbritain/roadmap-cli

Thin roadmap CLI/MCP wiring: reads meta.profile once and loads the matching executor package. The `roadmap` bin stays in the root package until the unshim slice.

Status: scaffold. Modules move in from `scripts/lib` one slice at a time; see
[`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md) and [`docs/roadmap/STATUS.md`](../../docs/roadmap/STATUS.md).
